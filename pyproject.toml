[project]
name = "committee-autoresearch"
version = "0.1.0"
description = "Autonomous research loop for non-linear committee machine learning a Hermite teacher"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "torch>=2.0.0",
    "wandb>=0.16.0",
    "matplotlib>=3.7.0",
    "requests>=2.28.0",
    "python-dotenv>=1.0.0",
]
