# Copy to .env and fill in your values:
#   cp .env.example .env

# Weights & Biases — leave unset to use `wandb login` instead of a key here
# WANDB_API_KEY=
WANDB_PROJECT=committee-autoresearch

# Telegram — create bot via @BotFather, chat id via @userinfobot
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
